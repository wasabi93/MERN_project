import { makeStyles } from '@material-ui/core/styles'


const useStyles = makeStyles(() => ({
    main: {
      padding: '10px',
    },
    gap: {
      gridGap: 20,
    }
  }));

export default useStyles